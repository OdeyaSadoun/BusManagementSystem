<Window x:Class="PlGui.LineShow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlGui"
        mc:Ignorable="d"
        Title="LineShow" Height="450" Width="800">
    <Grid>
        <!--<ListView x:Name="lvLines" Margin="62,60,68,45" ItemsSource="{Binding}" ItemTemplate="{StaticResource listOfLines}"
                  HorizontalContentAlignment="Stretch" MouseDoubleClick="Preview_Double_Click" HorizontalAlignment="Stretch"
                  Grid.Row="0" VerticalContentAlignment="Stretch"   FontSize="14" Grid.ColumnSpan="2" >
            <ListView.Resources>
                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="Height" Value="40"/>
                    <Style.Triggers>
                        --><!-- Sets the color of the row according to bus' status --><!--
                        <DataTrigger Binding="{Binding Status, Converter={StaticResource backCon}}" Value="Ready">
                            <Setter Property="Background" Value="LightGreen" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status, Converter={StaticResource backCon}}"  Value="Else">
                            <Setter Property="Background" Value="LightSalmon" />
                        </DataTrigger>

                    </Style.Triggers>
                </Style>
            </ListView.Resources>

        </ListView>-->
        <ListView x:Name="lvLines" HorizontalAlignment="Left" Height="100" Margin="358,187,0,0" ItemsSource="{Binding}" VerticalAlignment="Top" Width="100">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
